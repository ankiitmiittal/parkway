# Normalise line endings. Without this, editing on Windows (CRLF) and from a
# phone or web editor (LF) produces diffs where every line looks changed.
* text=auto eol=lf

# Binary: never touch these.
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
